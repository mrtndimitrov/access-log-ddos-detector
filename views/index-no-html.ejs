### TOTAL REQUESTS: <%= all %>
### TOTAL UNIQUES IPs: <%= unique %>

# TOP 20 (or > 1000 hits) IPs:
<%_ for(const ip of ips) { _%>
    <%_ if (ip.info && (ip.info.allowed || ip.info.blocked)) { continue; } _%>
<%= ip.i %>. <%_ if(ip.geoip){ _%><%= ip.geoip.geo.country_name %><%_ } else if (ip.country && ip.country.country) { _%><%= ip.country.country.iso_code %><%_ } _%>: <%= ip.address %>: <%= ip.num %>
<%_ } _%>
